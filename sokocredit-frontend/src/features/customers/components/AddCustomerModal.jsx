import { useState } from 'react'
export default function AddCustomerModal({ onClose, onSubmit }) { const [form, setForm] = useState({ name: '', business: '', market: '' }); const change = (key, value) => setForm({ ...form, [key]: value }); return <div className="modal-backdrop" role="presentation"><form className="modal" onSubmit={(event) => { event.preventDefault(); onSubmit(form.name) }}><button className="close" type="button" onClick={onClose}>×</button><h2>Add Customer</h2><p>Start a new field trader profile.</p><label>Full name<input required value={form.name} onChange={(event) => change('name', event.target.value)} placeholder="e.g. Jane Doe" /></label><label>Business type<input required value={form.business} onChange={(event) => change('business', event.target.value)} placeholder="e.g. Fresh Produce" /></label><label>Market<select required value={form.market} onChange={(event) => change('market', event.target.value)}><option value="">Select market</option><option>Gikomba</option><option>Muthurwa</option><option>City Park</option></select></label><button className="new-button submit" type="submit">Create customer</button></form></div> }
